# Repository Agent Instructions

## Git Authorship

- Set the author explicitly for every agent-created commit.
- Use the active agent's name as `user.name`.
- Use `<agent-name>@projectempyrean.org` as `user.email`, replacing
  `<agent-name>` with the lowercase agent name with spaces removed.
