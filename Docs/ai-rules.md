# Rules for AI Contributors

Be smart.

Specify the model used in generated content with the `model` attribute in `ai-gen` tags. For example:

```markdown
`<ai-gen model="Gemini 2.5 Flash Preview 9-25>">`
> ...
`</ai-gen>`
```